{
  "schema_version": 1,
  "metrics": {
    "production_loc": 2317,
    "production_files": 29,
    "production_packages": 10,
    "test_files": 14,
    "production_functions": 179,
    "decision_points": 325
  },
  "persistence": {
    "module": "go.etcd.io/bbolt",
    "entities": [
      "BridgeRecord",
      "WindScenarioRecord",
      "TrajectoryRecord",
      "ViewPreferenceRecord"
    ],
    "reopen_test": "TestPersistenceSurvivesReopen"
  },
  "workflows": [
    {
      "steps": [
        "register a bridge geometry",
        "save wind speed amplitude and sampling duration",
        "calculate and persist three dimensional trajectory points",
        "load the saved trajectory for inspection"
      ],
      "test": "TestWorkflowCaptureCalculatePersist"
    },
    {
      "steps": [
        "select a bridge from the catalog",
        "query saved trajectories for that bridge",
        "switch the projection to side view",
        "return projected points for display"
      ],
      "test": "TestWorkflowQueryAndProject"
    },
    {
      "steps": [
        "close the embedded database",
        "open the same database path again",
        "read the previously saved trajectory",
        "verify its samples remain available"
      ],
      "test": "TestWorkflowRecovery"
    }
  ]
}
